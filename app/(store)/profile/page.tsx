import { StoreProfilePageContainer } from "@/features/store-profile";

export const metadata = {
  title: "Perfil de la tienda — Ambulante",
};

export default function ProfilePage() {
  return <StoreProfilePageContainer />;
}
