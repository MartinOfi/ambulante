# B10-B — Tienda: operación (availability + location publishing + catálogo CRUD con imágenes)

> **Fase:** B10 — Swap tienda (features Tienda consumen backend real)
> **Goal de la fase:** Mismo objetivo que B9 pero del lado Tienda. Todos los mocks de dashboard, catálogo, availability, inbox, accept/reject/finalize quedan reemplazados.
> **Acceptance criteria de la fase:** Tienda puede loguear, registrarse y completar perfil con logo, activar disponibilidad y publicar ubicación, crear/editar productos con foto, ver pedidos entrantes en vivo, aceptar/rechazar/finalizar, ver analytics, recibir push.

> **Atajos:** [INDEX](../INDEX.md) · [convenciones](../convenciones.md) · [portabilidad](../portabilidad.md) · [decisiones](../decisiones.md)

- **Estado:** ✅ done [owner: chat-2026-05-05, closed: 2026-05-05]
- **Por qué:** Slice vertical "tienda operando": prende disponibilidad, transmite ubicación cada 30-60s mientras está activa, y administra el catálogo (productos con fotos). Las dos features están entrelazadas — un toggle de availability puede limpiar un buffer de location publishing, y el catálogo necesita estar visible en el detalle solo si la tienda está disponible. Diseñarlas juntas evita race conditions en el toggle.
- **Entregable:**
  1. **Availability + location publishing:** Hook `useAvailability` para el toggle server-side que actualiza `stores.available`. Hook `useLocationPublisher(storeId)` que lee `geolocation.watchPosition`, filtra lecturas con `accuracy <= 50` (PRD §7.5), INSERT en `store_locations` cada 30-60s. Buffer si offline.
  2. **Catálogo CRUD + image upload:** `features/catalog/` consume `ProductsRepository` (CRUD completo) + `StorageService.upload` (bucket `products`) + `resizeImageForUpload` de B5.3 antes del upload. Server Actions create/update/delete. Componente `ProductImageUpload` que cierra la integración pendiente del helper. Tests.
- **Archivos:** `features/store-dashboard/hooks/useAvailability.ts`, `features/store-dashboard/hooks/useLocationPublisher.ts`, `features/catalog/**` (incluye `ProductImageUpload`).
- **Depends on:** B3.1, B5.3, B10-A
- **Continues with:** B10-C
- **Skill rules aplicables:** `data-batch-inserts` (si se buffering)
- **REGISTRY:** `features.md`. Cierra la integración pendiente registrada en NT-29 (image-upload nullable dimensions) — evaluar si conviene aplicar el cambio de tipo acá.
- **Estimación:** XL
- **Notas:** (se llena al cerrar)
- **Tareas originales fusionadas:** B10.2 (availability + location), B10.3 (catálogo + image upload).
