# B11-C — Admin: user management

> **Fase:** B11 — Swap admin (features Admin consumen backend real)
> **Goal de la fase:** Reemplazar mocks del panel admin por queries reales. KPIs, validación de tiendas, moderación, audit log, user management.
> **Acceptance criteria de la fase:** Admin puede ver KPIs en vivo, aprobar/rechazar tiendas pendientes con documentos adjuntos, moderar contenido reportado, auditar pedidos históricos, gestionar usuarios.

> **Atajos:** [INDEX](../INDEX.md) · [convenciones](../convenciones.md) · [portabilidad](../portabilidad.md) · [decisiones](../decisiones.md)

- **Estado:** ✅ done [owner: chat-2026-04-29, closed: 2026-04-29]
- **Por qué:** F14.5 del frontend epic. Admin puede suspender/reactivar usuarios y ver sus pedidos asociados. Queda como tarea independiente porque tiene su propio modelo de permisos (suspend afecta auth + RLS, no es una transición de pedido) y su propio set de UI.
- **Entregable:** `features/user-management/` consume `UsersRepository`. Server Actions `suspendUser(userId, reason)` y `reactivateUser(userId)` con state machine de `users.suspension_status`. Tabla con filtros, vista de detalle con orders del usuario.
- **Archivos:** `features/user-management/**`.
- **Depends on:** B3.1
- **Continues with:** —
- **Skill rules aplicables:** —
- **REGISTRY:** `features.md`.
- **Estimación:** M
- **Notas:**
  - State machine TS-only sobre `users.suspended` (boolean) — sin migración SQL. `SUSPENSION_STATUS = "active" | "suspended"` consolidado con el constant huérfano `USER_SUSPENSION_STATUS` que ya estaba en `shared/constants/user-management.ts` (DRY).
  - **Decisión:** no se persiste `reason` en una columna nueva — queda como input del Server Action y se loguea via `serverLogger`. La persistencia formal la entregará B11-B (audit log e2e). Esto mantuvo el scope en M.
  - **Server Actions** (primer uso del patrón en este repo): `suspendUserAction({ userId, reason })`, `reactivateUserAction({ userId })`. Gate por `is_admin()` RPC. Usan `createRouteHandlerClient` (no `createServerClient`) porque las cookies son writable en Server Actions, igual que en Route Handlers.
  - **Portabilidad:** ningún import `@supabase/*` fuera de los 3 dirs permitidos. Server actions instancian `SupabaseUserRepository` + `SupabaseOrderRepository` via barrel `@/shared/repositories`.
  - **Filtros:** `useUserManagementFilters` usa `nuqs` para URL state (`role`, `status`, `q`). Sentinel `"all"` ↔ `null` para "sin filtro". Búsqueda por email/nombre client-side (no roundtrip al server).
  - **Detail view:** ruta `app/(admin)/admin/users/[userId]/page.tsx` con `UserDetailPageContainer`. Trae user + orders del usuario. Action buttons en el detail (suspend/reactivate).
  - **Archivos creados:**
    - `shared/domain/user-suspension.ts` (+ test)
    - `features/user-management/server-actions/user-management-actions.ts` (+ test)
    - `features/user-management/services/userManagement.factory.ts`
    - `features/user-management/hooks/useReactivateUserMutation.ts`, `useUserDetailQuery.ts`, `useUserManagementFilters.ts`
    - `features/user-management/components/UserDetailPage/`, `UserOrdersTable/`, `UserFiltersBar/`
    - `app/(admin)/admin/users/[userId]/page.tsx`
  - **Archivos modificados:**
    - `shared/schemas/user-management.ts` (rename `reinstate→reactivate`, add `reason` y `userDetailQuerySchema`)
    - `features/user-management/services/userManagement.service.ts` (state machine + getUserDetail)
    - `features/user-management/components/UserManagementPage/**`, `UserTable/**`, `SuspendConfirmDialog/**`
    - `features/user-management/hooks/useUsersQuery.ts` (filters), `useSuspendUserMutation.ts` (Server Action)
    - `features/user-management/index.ts`, `types/index.ts`
    - `shared/REGISTRY.md` + `shared/REGISTRY-detail/features.md`
  - **Archivos eliminados:** `useReinstateUserMutation.ts`
  - **Verificación final (post code-review fixes):** tsc 0 errors · vitest 73/73 (state machine + service + page + table + server actions + supabase repo) · max archivo 142 líneas · 0 imports `@supabase/*` fuera de dirs permitidos.
  - **Fails pre-existentes en main (no introducidos por esta task):** `shared/services/index.test.ts` (8) y `shared/services/auth.test.ts` (4) por env-var rotation. Verificado corriendo los mismos tests en `main` antes del worktree.
  - **Code review (1 pasada):** 1 CRITICAL + 3 HIGH + 4 MEDIUM + 3 LOW. Fixes aplicados:
    - **CRITICAL:** Domain error class `UserManagementDomainError` para que `toUserMessage` no propague mensajes raw del repo Supabase al cliente. Service y state machine throw `UserManagementDomainError`; el resto colapsa a "No se pudo completar la operación.".
    - **HIGH:** Cap defensivo `MAX_USERS_PER_QUERY = 500` en `SupabaseUserRepository.findAll` (paginación real diferida → NT-30); copy del dialog cambiado de "Motivo (queda registrado)" a "Motivo de la suspensión" para no prometer persistencia que B11-B entregará via audit log; `useUsersQuery` queryKey simplificada (prefix-match con `users.all()` confirmado).
    - **MEDIUM:** lookup de user en `usersQuery.data` (no en `visibleUsers`) — evita silent no-op si filtro oculta al target durante el click; UserDetailPage error path ahora usa `instanceof UserManagementDomainError` también; `logger.info` ya no incluye `reason` (solo `reasonLength`) — evita posible PII en logs futuros via Sentry; focus trap en SuspendConfirmDialog → diferido a NT-31 (patrón cross-feature, mejor refactor agrupado).
    - **LOW:** removido `?? null` cosmético en queryKey; `key="skeleton-${i}"` en lugar de index puro.
  - **Tests añadidos durante review:** `users.supabase.test.ts` ahora cubre el cap defensivo (`applies a defensive row cap`).
  - **NTs creados:** NT-30 (paginación real users), NT-31 (focus trap modales admin).
- **Tareas originales fusionadas:** B11.5 (rename — sin fusión).
