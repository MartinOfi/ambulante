# B10-C — Tienda: manejo de pedidos (inbox realtime + accept/reject/finalize)

> **Fase:** B10 — Swap tienda (features Tienda consumen backend real)
> **Goal de la fase:** Mismo objetivo que B9 pero del lado Tienda. Todos los mocks de dashboard, catálogo, availability, inbox, accept/reject/finalize quedan reemplazados.
> **Acceptance criteria de la fase:** Tienda puede loguear, registrarse y completar perfil con logo, activar disponibilidad y publicar ubicación, crear/editar productos con foto, ver pedidos entrantes en vivo, aceptar/rechazar/finalizar, ver analytics, recibir push.

> **Atajos:** [INDEX](../INDEX.md) · [convenciones](../convenciones.md) · [portabilidad](../portabilidad.md) · [decisiones](../decisiones.md)

- **Estado:** ✅ done [owner: chat-2026-04-30, closed: 2026-04-30]
- **Por qué:** Slice vertical "tienda procesando pedidos": inbox en vivo + transiciones de estado (aceptar / rechazar / finalizar). Las dos features comparten el realtime channel y el modelo de Order — el inbox suscribe a INSERT, las transiciones a UPDATE. Tienen que coordinar el invalidate del cache para no mostrar estados stale al usuario (T5, T6 y T7 del PRD).
- **Entregable:**
  1. **Inbox + realtime:** `features/store-dashboard/components/IncomingOrders/` consume `OrdersRepository.findByStoreAndStatus('RECIBIDO')` + subscription realtime al canal `orders:store_id=$id`. Notificación sonora opcional al recibir un pedido nuevo.
  2. **State transitions:** Server Actions `acceptOrder`, `rejectOrder`, `finalizeOrder` en `features/orders/actions.ts` (lado tienda). Cada una es transacción corta con state machine check, validación de rol, y emite domain event post-commit (triggers B8 push para notificar al cliente).
- **Archivos:** `features/store-dashboard/components/IncomingOrders/**`, `features/orders/actions.ts` (lado tienda), tests.
- **Depends on:** B6.3, B9-B
- **Continues with:** B10-D
- **Skill rules aplicables:** `lock-short-transactions`
- **REGISTRY:** `features.md`.
- **Estimación:** L
- **Notas:** Creados: `supabase/migrations/20260430120000_b10c_store_order_transitions_rpc.sql` (tres RPCs security definer: accept/reject/finalize con FOR UPDATE + LIMIT 1), `features/orders/store-transitions.constants.ts`, `features/orders/store-transitions.schemas.ts`, `features/orders/store-actions.test.ts` (17 tests Server Actions), `features/orders/hooks/useAcceptOrderMutation.ts`, `features/orders/hooks/useRejectOrderMutation.ts`, `features/orders/hooks/useFinalizeOrderMutation.ts` (con optimistic updates). Modificados: `features/orders/actions.ts` (Server Actions con Zod safeParse sobre shape RPC), `features/orders/components/OrderActions/OrderActions.container.tsx`, `features/orders/components/IncomingOrdersInbox/IncomingOrdersInbox.container.tsx`. REGISTRY actualizado en `shared/REGISTRY-detail/features.md`.
- **Tareas originales fusionadas:** B10.4 (inbox + realtime), B10.5 (accept/reject/finalize).
