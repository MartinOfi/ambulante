# B12-A — Observability: structured logging con request IDs + slow query alerts

> **Fase:** B12 — Observability backend
> **Goal de la fase:** Instalar la visibilidad mínima para diagnosticar y alertar sobre problemas de performance / errores.
> **Acceptance criteria de la fase:** Panel admin permite leer top slow queries de `pg_stat_statements`; Sentry recibe queries >100ms mean y errores con request ID propagado; logs server-side son estructurados con request IDs.

> **Atajos:** [INDEX](../INDEX.md) · [convenciones](../convenciones.md) · [portabilidad](../portabilidad.md) · [decisiones](../decisiones.md)

- **Estado:** ✅ done [owner: chat-2026-04-28, closed: 2026-04-28]
- **Por qué:** Cierra la fase B12 con un slice operativo: structured logging (request IDs propagados) + alertas (Sentry recibe los slow queries detectados por el panel B12.1). Sin request IDs, las alertas son anónimas y dificultan la correlación con el log del request original — por eso conviene diseñar las dos cosas juntas.
- **Entregable:**
  1. **Structured logging:** middleware que inyecta `X-Request-Id` header; helper `logger.withRequestId(req)` integrado en Route Handlers de B7/B8/B12 y en Server Actions críticas.
  2. **Slow query alerts:** cron `/api/cron/check-slow-queries` cada 5 min que lee `pg_stat_statements`, compara con baseline, y manda breadcrumb/event a Sentry si un query cruzó el umbral (100ms mean o +50% vs semana pasada). El payload del evento incluye el request ID si está disponible.
  3. Schedule del cron incorporado a la migración de B7.1 vía patch.
- **Archivos:** `middleware.ts` (patch), `shared/services/logger.ts` (patch), `shared/utils/server-logger.ts` (patch si aplica), `app/api/cron/check-slow-queries/route.ts`, `supabase/migrations/<ts>_observability_alerts.sql` (patch a schedule_crons).
- **Depends on:** B12.1, B7.1
- **Continues with:** —
- **Skill rules aplicables:** `monitor-pg-stat-statements`
- **REGISTRY:** `data.md` (sección Logger).
- **Estimación:** L
- **Notas:**
  - **Archivos nuevos:**
    - `shared/utils/request-id.ts` (+ test) — header parsing/validation/generation, defensa contra header-injection (CR/LF/NUL, max 128 chars).
    - `shared/constants/observability.ts` — `REQUEST_ID_HEADER`, `SLOW_QUERY_THRESHOLD_MS=100`, `SLOW_QUERY_BASELINE_FACTOR=1.5`, `SLOW_QUERY_MAX_RESULTS=50`, `QUERY_TEXT_MAX_LENGTH=500`, `BASELINE_MIN_AGE_DAYS=6`.
    - `app/api/cron/check-slow-queries/route.ts` (+ test) — POST handler con bearer auth, RPC `internal.get_slow_queries_for_alerts`, parse Zod, alerta vía `reportSlowQuery` por cada breach.
    - `supabase/migrations/20260429000000_observability_alerts.sql` — tabla `slow_query_baselines` + RLS, función `internal.snapshot_slow_query_baseline()`, función `internal.get_slow_queries_for_alerts(p_threshold_ms, p_limit)`, schedule `check-slow-queries` (`*/5 * * * *`) y `snapshot-slow-query-baseline` (`0 3 * * 0`).
  - **Archivos modificados:**
    - `middleware.ts` — inyecta `x-request-id` en request forwarded headers + tagging del response (incluye redirects y rate-limit rejections).
    - `shared/utils/server-logger.ts` — nueva API `getOrCreateRequestLogger(headers | request)`.
    - `shared/utils/sentry.ts` — nueva API `reportSlowQuery(payload)` con `Sentry.captureMessage(level=warning)` + tags `breach_kind`/`request_id`.
    - `shared/REGISTRY.md` + `shared/REGISTRY-detail/infra.md` (Logger live ahí, no en `data.md` como decía el spec stale).
  - **Decisiones:**
    - **Sentry para slow queries usa `captureMessage(level=warning)`** y no `captureException` — son señales operativas, no errores de runtime; mezclarlos con el feed de errores degrada la oncall experience.
    - **Snapshot/reader agregan por queryid** — pg_stat_statements emite una fila por `(queryid, dbid, userid)`, sin agregación había PK conflict y reader retornaba duplicados.
    - **`extensions.pg_stat_statements`**, no `pg_catalog` — Supabase instala la extensión en `extensions` schema; con `set search_path = ''` la referencia debe ser explícita.
    - **REGISTRY entry en `infra.md` §5**, no `data.md` — el campo del task spec era stale; los Logger entries pre-existentes ya viven en infra.md.
  - **Migraciones aplicadas** (vía psql directo a la instancia local; `pnpm supabase db reset` está bloqueado por dos pares de migraciones con prefijo duplicado en `20260428000004` y `20260428000007`, pre-existentes a B12-A y fuera de scope — registrado como **NT-30** en `docs/NEXT-TASK.md`).
  - **Tests:** request-id (13), server-logger (3 nuevos), middleware (4 nuevos), check-slow-queries route (8). Total nuevos: 28. Suite afectada: 60/60 verde.
  - **Verificación SQL end-to-end:** snapshot inserta 820 baselines únicas; reader devuelve 5 breaches con `breach_kind="absolute"` (esperado — baseline aún < 6 días para activar el path de regresión).
- **Tareas originales fusionadas:** B12.2 (slow query alerts), B12.3 (structured server-side logging).
