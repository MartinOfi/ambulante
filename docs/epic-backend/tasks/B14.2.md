# B14.2 — Pipeline CI: preview DB por PR (Supabase branching opcional) → approval → prod

> **Fase:** B14 — Deploy producción
> **Goal de la fase:** Llevar el backend a Supabase Cloud, configurar secrets, pipeline de migraciones con approval, y cerrar go-live checklist.
> **Acceptance criteria de la fase:** Supabase project de prod existe con secrets inyectados; cada PR que modifica `supabase/migrations/` corre preview contra un branch; merge a main despliega via release-please; checklist de disaster recovery al día.

> **Atajos:** [INDEX](../INDEX.md) · [convenciones](../convenciones.md) · [portabilidad](../portabilidad.md) · [decisiones](../decisiones.md)

- **Estado:** ✅ done [closed: 2026-05-05 — deploy-prod.yml entregado via merge t2/b14-2-ci-pipeline]
- **Por qué:** Mergear una migración directo a prod es peligroso. Pipeline con preview + approval lo mitiga.
- **Entregable:** Workflow `deploy-prod.yml` que: (a) en PR, crea un branch de Supabase + corre migraciones + tests E2E; (b) en merge, requiere approval humano; (c) al aprobar, corre migraciones contra prod via `supabase db push`. Si Supabase Branching no está disponible en el tier contratado, el preview corre en Docker local en el runner.
- **Archivos:** `.github/workflows/deploy-prod.yml`.
- **Depends on:** B14.1, B0.4
- **Continues with:** —
- **Skill rules aplicables:** —
- **REGISTRY:** —
- **Estimación:** L
- **Notas:** El workflow fue desarrollado en la branch `t2/b14-2-ci-pipeline` y mergeado a main el 2026-05-05 junto con el fix de `server-only` en `next.config.ts`. Incluye preview con Supabase Branching + fallback Docker, approval gate vía GitHub environment `production-approval`, y deploy con `supabase db push`.
