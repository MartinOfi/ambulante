# B5.4 — Flow admin: revisar documentos de validación

> **Fase:** B5 — Storage (buckets + RLS + upload helpers)
> **Goal de la fase:** Dejar los 3 buckets del producto operativos con RLS correctamente configurada y helpers reutilizables para upload con validación.
> **Acceptance criteria de la fase:** Buckets `products`, `store-logos`, `validation-docs` existen con policies; el facade `StorageService` apunta a Supabase; upload helper valida tamaño, mime type, y orienta al user si algo falla; tests de integración cubren los 3 buckets.

> **Atajos:** [INDEX](../INDEX.md) · [convenciones](../convenciones.md) · [portabilidad](../portabilidad.md) · [decisiones](../decisiones.md)

- **Estado:** ✅ done [owner: chat-2026-04-28, closed: 2026-04-28]
- **Por qué:** El bucket `validation-docs` existe, pero sin una UI admin para revisarlos no se aprovecha. Esta tarea cierra el loop con `features/store-validation/`.
- **Entregable:** Hook `useValidationDoc(storeId, docType)` que genera signed URL + componente `<ValidationDocViewer>` en `features/store-validation/`. Integración con `features/admin-store-validation` para mostrar docs al revisar una tienda pendiente.
- **Archivos:** `features/store-validation/hooks/useValidationDoc.ts`, `features/store-validation/components/ValidationDocViewer/`.
- **Depends on:** B5.2
- **Continues with:** —
- **Skill rules aplicables:** —
- **REGISTRY:** `features.md`.
- **Estimación:** M
- **Notas:** Hook `useValidationDoc(storeId, docType)` en `features/store-validation/hooks/useValidationDoc.ts` — encadena `storeValidationService.getValidationDoc` (nueva en la interfaz) con `storageService.getSignedUrl` contra el bucket `validation-docs`; signed URL TTL 1h, staleTime 55min. Componente `ValidationDocViewer` (dumb) ramifica entre PDF (`<iframe>`) e imagen (`<Image fill unoptimized>` — `unoptimized` evita que el optimizer de Next strip el query string del signed URL); estados loading/error/empty/malformed. `ValidationDocViewerContainer` smart conecta el hook. Integrado en `StoreDetailPanel` via slot `validationDocsSlot?: ReactNode` (se mantiene el dumb puro), inyectado por `StoreDetailPanel.container.tsx` con los 3 doc types (`id_front`, `id_back`, `business_proof`). Constants nuevas: `VALIDATION_DOC_TYPES`, `VALIDATION_DOC_TYPE_LABELS`, `VALIDATION_DOC_SIGNED_URL_EXPIRES_IN_S`, `VALIDATION_DOC_STALE_TIME_MS`, `VALIDATION_DOC_MIME_TYPE_PDF`. Types: `ValidationDocType`, `ValidationDocMeta { path, mimeType, filename }`, `ValidationDocs = Partial<Record<ValidationDocType, ValidationDocMeta>>`, `GetValidationDocInput`. Service mock siembra `documents` en las 2 tiendas pendientes (1 con los 3 docs, otra solo con `id_front` para cubrir empty state). Nuevo barrel `features/store-validation/services/index.ts` para no acoplar el hook al `.mock.ts` directo (review HIGH fix). Query key nueva: `queryKeys.stores.validationDoc(storeId, docType)`. **Code review 1-pass aplicó:** HIGH (importar service vía barrel), MEDIUM (deep-copy `documents` en constructor del mock), LOW (MIME PDF a constants, guard defensivo `mimeType=null` en dumb, test de slot en container). 17 tests nuevos (3 service, 3 hook, 8 viewer, 3 panel slot); 62/62 store-validation tests pasan; tsc 0 errors. Pre-existing failures en `shared/services/index.test.ts` ya cubiertos por NT-32 (no bloqueante).

---
