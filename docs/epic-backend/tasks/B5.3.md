# B5.3 — Upload helper con optimización de imagen

> **Fase:** B5 — Storage (buckets + RLS + upload helpers)
> **Goal de la fase:** Dejar los 3 buckets del producto operativos con RLS correctamente configurada y helpers reutilizables para upload con validación.
> **Acceptance criteria de la fase:** Buckets `products`, `store-logos`, `validation-docs` existen con policies; el facade `StorageService` apunta a Supabase; upload helper valida tamaño, mime type, y orienta al user si algo falla; tests de integración cubren los 3 buckets.

> **Atajos:** [INDEX](../INDEX.md) · [convenciones](../convenciones.md) · [portabilidad](../portabilidad.md) · [decisiones](../decisiones.md)

- **Estado:** ✅ done [owner: chat-2026-04-28, closed: 2026-04-28]
- **Por qué:** Subir fotos directas desde móvil llega con 3-8MB frecuentemente. Resize client-side antes de upload ahorra ancho de banda y storage. Es un detalle de UX/perf que paga rápido.
- **Entregable:** `shared/utils/image-upload.ts` con `resizeImageForUpload(file, maxDim=1600)` usando `createImageBitmap` + canvas. Tests con fixtures. Integración en `features/catalog/components/ProductImageUpload`.
- **Archivos:** `shared/utils/image-upload.ts`, `shared/utils/image-upload.test.ts`.
- **Depends on:** B5.2
- **Continues with:** —
- **Skill rules aplicables:** —
- **REGISTRY:** `infra.md` (sección Utils).
- **Estimación:** M
- **Notas:** Implementado `shared/utils/image-upload.ts` con `resizeImageForUpload(file, options?)` usando `createImageBitmap` + canvas + `toBlob`. DI via `options.deps` (`createImageBitmap`, `createCanvas`) — patrón alineado con `storage.supabase.ts` para evitar `vi.stubGlobal`. Defaults: `maxDimension=1600`, `mimeType="image/jpeg"`, `quality=0.85` (clamp [0,1]). No-op si el archivo está dentro del límite o si el MIME no es redimensionable (constante `RESIZABLE_IMAGE_TYPES = ["image/jpeg", "image/png", "image/webp", "image/gif"]`). Renombra extensión al cambiar MIME (ej. `mi-foto.png` → `mi-foto.jpg`). `ImageBitmap.close()` siempre vía `try/finally` para liberar memoria GPU. 13 vitest tests cubriendo: defaults, no-op (dentro del límite y MIME no redimensionable), resize horizontal/vertical, custom maxDim/mime/quality, cierre del bitmap, bitmap sin `.close`, preservación de nombre, error en decode, error con blob null, clamp de quality. Code review 1-pass (quick-finish): 0 CRITICAL, 0 HIGH. Fixes aplicados: MEDIUM 1 (`computeOutputDimensions` retorna nuevo objeto en vez de alias del input), LOW 1 (eliminado field `drawnFrom` dead code en harness de tests), LOW 2 (test agregado para bitmap sin `.close`). MEDIUM 2 (tipar dimensions como nullable en no-op path) registrado como NT-29 — se evalúa al integrar en B10.3. **Integración consumidora pendiente para B10.3** (`features/catalog/components/ProductImageUpload` no existe todavía).
