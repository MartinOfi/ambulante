# B5.2 — Implementación del facade `StorageService`

> **Fase:** B5 — Storage (buckets + RLS + upload helpers)
> **Goal de la fase:** Dejar los 3 buckets del producto operativos con RLS correctamente configurada y helpers reutilizables para upload con validación.
> **Acceptance criteria de la fase:** Buckets `products`, `store-logos`, `validation-docs` existen con policies; el facade `StorageService` apunta a Supabase; upload helper valida tamaño, mime type, y orienta al user si algo falla; tests de integración cubren los 3 buckets.

> **Atajos:** [INDEX](../INDEX.md) · [convenciones](../convenciones.md) · [portabilidad](../portabilidad.md) · [decisiones](../decisiones.md)

- **Estado:** ✅ done [owner: chat-2026-04-28, closed: 2026-04-28]
- **Por qué:** Los componentes de upload (ya existentes en features/catalog y features/store-profile) consumen `StorageService`. Necesitamos la implementación real.
- **Entregable:** `shared/services/storage.supabase.ts` con `upload`, `getPublicUrl`, `getSignedUrl`, `remove`. Validación de tamaño + mime type integrada. Conversión de errores Supabase a errores de dominio (`StorageError`).
- **Archivos:** `shared/services/storage.supabase.ts`, `shared/services/storage.supabase.test.ts`.
- **Depends on:** B5.1, B3.2
- **Continues with:** —
- **Skill rules aplicables:** —
- **REGISTRY:** `data.md`.
- **Estimación:** M
- **Notas:** Implementado `shared/services/storage.supabase.ts` con factory `createSupabaseStorageService(client?)` + singleton `supabaseStorageService`. DI via `SupabaseStorageBucket` / `SupabaseStorageClient` interfaces (sin `vi.mock` en tests). Validación pre-red: tamaño vs `STORAGE_SIZE_LIMITS`, MIME vs `STORAGE_ALLOWED_MIME_TYPES`; lookup tables tipadas `Record<StorageBucket, …>` para exhaustividad en compilación. Client cacheado en `resolvedClient` (patrón de `realtime.supabase.ts`). Se añadió `GetSignedUrlParams` + `getSignedUrl` a `storage.types.ts` y mock. 15 vitest tests. `index.test.ts` actualizado: stub "throws TODO" → "returns StorageResult". 2-pass code review: 0 CRITICAL, 0 HIGH post-fix.
