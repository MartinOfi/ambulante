# B10-A — Tienda: onboarding completo (auth + alta + perfil con logo)

> **Fase:** B10 — Swap tienda (features Tienda consumen backend real)
> **Goal de la fase:** Mismo objetivo que B9 pero del lado Tienda. Todos los mocks de dashboard, catálogo, availability, inbox, accept/reject/finalize quedan reemplazados.
> **Acceptance criteria de la fase:** Tienda puede loguear, registrarse y completar perfil con logo, activar disponibilidad y publicar ubicación, crear/editar productos con foto, ver pedidos entrantes en vivo, aceptar/rechazar/finalizar, ver analytics, recibir push.

> **Atajos:** [INDEX](../INDEX.md) · [convenciones](../convenciones.md) · [portabilidad](../portabilidad.md) · [decisiones](../decisiones.md)

- **Estado:** ✅ done [owner: chat-2026-04-30, closed: 2026-04-30]
- **Por qué:** Slice vertical "alta de tienda completa": auth + onboarding multi-step + perfil. El alta (T1 del PRD) sube docs de validación (B5.4) y crea registro `pending_validation`; el perfil (T6) edita nombre/descripción/horarios y sube logo. Son la misma feature de "tienda armando su presencia" — diseñarlas separadas obliga a duplicar el wiring de `StorageService` y dos flows distintos de upload.
- **Entregable:**
  1. **Auth + onboarding:** `features/store-onboarding/` conectada a auth real + upload de docs de validación via `StorageService` (bucket `validation-docs`) + crear registro en `stores` con `status = pending_validation`.
  2. **Profile + logo:** `features/store-profile/` con edit form (nombre, descripción, horarios) + upload de logo via `StorageService` (bucket `store-logos`).
- **Archivos:** `features/store-onboarding/**`, `features/store-profile/**`.
- **Depends on:** B4.4, B5.4, B5.2
- **Continues with:** B10-B
- **Skill rules aplicables:** —
- **REGISTRY:** `features.md`.
- **Estimación:** XL
- **Notas (cerrado chat-2026-04-30):**
  - Archivos creados/modificados: `features/store-onboarding/schemas/store-onboarding.schemas.ts`, `features/store-onboarding/services/submit-store-onboarding.ts` + test, `features/store-onboarding/server-actions/store-onboarding-actions.ts`, `shared/schemas/store.ts`, `shared/constants/store.ts`, `shared/repositories/supabase/stores.supabase.ts`, `shared/repositories/supabase/mappers.ts`, `supabase/migrations/20260430000001_b10_a_cuit_and_view_update.sql`, `supabase/tests/b10_a_validation_status.sql` (10 assertions).
  - `STORE_STATUS.closed` usado en lugar de magic string. `SUPABASE_SERVICE_ROLE_KEY` guard falla con error claro si ausente. NT-40 registrado (3-round-trip sin transaction en `SupabaseStoreRepository.create`).
  - `stores_view` recreada (sin ALTER) al agregar `cuit` — security_invoker/security_barrier se preservan.
  - Stale test `realtimeService.subscribe throws TODO` corregido (ahora verifica que devuelve función).
  - B10-A.2b (upload docs validación) y B10-A.3 (store-profile swap) diferidos a scope propio.
- **Notas parciales (chat-2026-04-29, hasta `2a482a97`):**
  - ✅ **B10-A.1 cerrado** — migración `store_validation_status` enum + columna NOT NULL default `pending` + index `(validation_status, created_at desc)` para queue admin. pgTAP test `b10_a_validation_status.sql` (6 assertions) verde. Aplicada y verificada en local.
  - ✅ **B10-A.2a cerrado** — Server Action `submitStoreOnboardingAction` (en `features/store-onboarding/server-actions/store-onboarding-actions.ts`) + pure function `submitStoreOnboarding` con DI deps (10 unit tests). Service `services/store-onboarding.ts` swap de mock → real con lazy import (evita carga de `next/headers` en jsdom). Wizard test fixtures actualizados a la nueva interface (`{ success: true, storeId }`). Tests del feature: 52/52 verde, typecheck limpio.
  - ⚪ **B10-A.2b pendiente** — upload de docs de validación (`validation-docs` bucket vía `StorageService.upload`) + nuevo step 4 en wizard (`StepDocs.container.tsx`) que recolecta DNI frente/dorso + habilitación. Sin esto, el flow admin de B5.4 ve pending stores con storage paths vacíos.
  - ⚪ **B10-A.3 pendiente** — features/store-profile swap del mock + Server Action `updateStoreLogo` (bucket `store-logos`) + eliminar `MOCK_STORE_ID`.
  - **Findings registrados como NT (descubiertos por esta tarea):**
    - **NT-38** — bug `auth.supabase.ts.toUser()` devuelve `auth.uid()` en vez de `public.users.public_id`. Mi Server Action lo workaroundea con un lookup interno, pero hay que arreglarlo en la capa de auth **antes de B10-B** (cuando location publishing real lo necesite).
    - **NT-39** — `Store` Zod schema demasiado rígido: `photoUrl/tagline/priceFromArs` deberían ser opcionales. Workaround actual: placeholders en `PENDING_STORE_PLACEHOLDER` dentro de `submit-store-onboarding.ts`. Resolver junto con B10-A.3.
  - **Decisión scope**: continuar A.2b + A.3 en su propio worktree para mantener el PR de A.1 + A.2a reviewable (~315 líneas, 1 review pass).
- **Tareas originales fusionadas:** B10.1 (auth + onboarding), B10.6 (profile + logo).
