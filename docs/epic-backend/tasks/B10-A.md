# B10-A — Tienda: onboarding completo (auth + alta + perfil con logo)

> **Fase:** B10 — Swap tienda (features Tienda consumen backend real)
> **Goal de la fase:** Mismo objetivo que B9 pero del lado Tienda. Todos los mocks de dashboard, catálogo, availability, inbox, accept/reject/finalize quedan reemplazados.
> **Acceptance criteria de la fase:** Tienda puede loguear, registrarse y completar perfil con logo, activar disponibilidad y publicar ubicación, crear/editar productos con foto, ver pedidos entrantes en vivo, aceptar/rechazar/finalizar, ver analytics, recibir push.

> **Atajos:** [INDEX](../INDEX.md) · [convenciones](../convenciones.md) · [portabilidad](../portabilidad.md) · [decisiones](../decisiones.md)

- **Estado:** 🟡 in-progress [owner: chat-2026-04-29, started: 12:54]
- **Por qué:** Slice vertical "alta de tienda completa": auth + onboarding multi-step + perfil. El alta (T1 del PRD) sube docs de validación (B5.4) y crea registro `pending_validation`; el perfil (T6) edita nombre/descripción/horarios y sube logo. Son la misma feature de "tienda armando su presencia" — diseñarlas separadas obliga a duplicar el wiring de `StorageService` y dos flows distintos de upload.
- **Entregable:**
  1. **Auth + onboarding:** `features/store-onboarding/` conectada a auth real + upload de docs de validación via `StorageService` (bucket `validation-docs`) + crear registro en `stores` con `status = pending_validation`.
  2. **Profile + logo:** `features/store-profile/` con edit form (nombre, descripción, horarios) + upload de logo via `StorageService` (bucket `store-logos`).
- **Archivos:** `features/store-onboarding/**`, `features/store-profile/**`.
- **Depends on:** B4.4, B5.4, B5.2
- **Continues with:** B10-B
- **Skill rules aplicables:** —
- **REGISTRY:** `features.md`.
- **Estimación:** XL
- **Notas:** (se llena al cerrar)
- **Tareas originales fusionadas:** B10.1 (auth + onboarding), B10.6 (profile + logo).
