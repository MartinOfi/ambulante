# B13-A — Hardening: rate limiting in-DB + security smoke tests en CI

> **Fase:** B13 — Hardening (rate limiting real, seed data, runbooks)
> **Goal de la fase:** Llevar el backend de "funciona en dev" a "soporta tráfico real sin descuidos operacionales".
> **Acceptance criteria de la fase:** Rate limiting real sobre endpoints sensibles; seed data útil para devs nuevos; runbooks para los escenarios de incidente más probables; security smoke tests corriendo en CI.

> **Atajos:** [INDEX](../INDEX.md) · [convenciones](../convenciones.md) · [portabilidad](../portabilidad.md) · [decisiones](../decisiones.md)

- **Estado:** 🟡 in-progress [owner: chat-2026-04-29, started: 11:07]
- **Por qué:** Slice de seguridad operacional: el rate limiting real (in-DB con leaky bucket) y los tests que verifican que existe en endpoints críticos van juntos. Implementar uno sin el otro no agrega valor — el rate limiter sin tests se rompe en silencio, los tests sin implementación reportan red constante.
- **Entregable:**
  1. **Rate limiting in-DB:** tabla `rate_limit_buckets` + función `check_rate_limit(key text, max int, window_seconds int)` (leaky bucket). Middleware o wrappers por endpoint que la invoquen en submit order, push subscribe, auth attempts. Implementación de `shared/services/rate-limit.supabase.ts` que sustituye al `InMemoryRateLimiter` actual.
  2. **Security smoke tests en CI:** suite en `e2e/security/` con (a) fuzzing de inputs en Server Actions críticas; (b) intento de leer tablas restringidas con anon role y expectar vacío/error; (c) flood de 100 req/s a auth/submit-order y expectar 429. Job separado en `.github/workflows/ci.yml`.
- **Archivos:** `supabase/migrations/<ts>_rate_limit.sql`, `shared/services/rate-limit.supabase.ts`, integración en middleware y/o por-endpoint, `e2e/security/*.spec.ts`, `.github/workflows/ci.yml` (patch).
- **Depends on:** B3.1, B2.3
- **Continues with:** —
- **Skill rules aplicables:** `lock-short-transactions`, `lock-advisory`
- **REGISTRY:** `data.md` (rate limit service supabase), `testing.md` (security smoke tests).
- **Estimación:** XL
- **Notas:**
  - **Migración:** `supabase/migrations/20260429141719_rate_limit.sql` — tabla `public.rate_limit_buckets` (token bucket con `key text PK`, `tokens double precision`, `last_refill_at`, `updated_at` + índice por `updated_at`); función `check_rate_limit(p_key, p_max_requests, p_window_seconds)` con `pg_advisory_xact_lock(hashtext(p_key))` para serialización por key (skill `lock-advisory`); función `cleanup_rate_limit_buckets(p_ttl_seconds)`. RLS deny-all + grants service_role.
  - **Servicio TS:** `shared/services/rate-limit.supabase.ts` — `SupabaseRateLimiter implements RateLimitService`; fail-open + log si la RPC falla (rate limit es defense-in-depth, no auth — un Supabase caído no debe tirar la app); validación zod del shape de respuesta. Tests en `rate-limit.supabase.test.ts` (7 casos: input mapping, output mapping, blocked, 3 paths de fail-open, conversión windowMs→seconds).
  - **Factory env-aware:** `shared/services/rate-limit.factory.ts` — `createRateLimiterFromEnv()` retorna Supabase si `NEXT_PUBLIC_SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY` están seteadas, sino `InMemoryRateLimiter`. Tests en `rate-limit.factory.test.ts` (4 casos).
  - **Wire:** `middleware.ts` cambia `createRateLimitService` → `createRateLimiterFromEnv`. `middleware.test.ts` actualiza el mock target. 13/13 tests existentes siguen verdes.
  - **pgTAP:** `supabase/tests/rate_limit.sql` — 11 casos: primer hit consume 1, burst capacity, blocked sobre capacity, refill después del window simulado backdating `last_refill_at`, isolation entre keys, `cleanup_rate_limit_buckets` borra solo viejos, params inválidos throws, RLS deny-all anon/authenticated. 99/99 pgTAP suite pasa.
  - **Security smoke (B13-A pieza 2):** `e2e/security/{rate-limit-flood,server-action-fuzz,rls-anon-restricted}.spec.ts`. Config separado `playwright.security.config.ts` (port 3110) + script `pnpm test:e2e:security`. Job nuevo `security-smoke` en `.github/workflows/ci.yml` boota Supabase + exporta env + corre la suite.
  - **REGISTRY:** `infra.md §12` extendida con `SupabaseRateLimiter`, `RateLimitRpcClient`, `createRateLimiterFromEnv` y la nueva migration; `testing.md §15` lista `rate_limit.sql`; nuevo `testing.md §17` documenta la suite security smoke; índice rápido en `REGISTRY.md` con 6 entradas nuevas.
  - **Fix encontrado fuera de scope (commiteado aparte):** `pnpm supabase db reset` bombeaba por dos pares de migrations con timestamp duplicado (`20260428000004` y `20260428000007`). Renombré `store_profile_extras` → `0008` y `get_top_slow_queries_fn` → `0009`. Registrado en NT-30 (guard CI) y commiteado como `fix(migrations): renombrar timestamps duplicados en B3.1 y B12.1`.
  - **Pre-existente registrado:** ~18 tests fallan en `pnpm test` por module-load throw cuando `NEXT_PUBLIC_SUPABASE_URL` no está seteado — registrado como NT-31. No fue introducido por esta tarea.
  - **Skill rules aplicadas:**
    - `lock-advisory` — `pg_advisory_xact_lock(hashtext(p_key))` evita `FOR UPDATE` sobre row inexistente en el primer hit y se libera al commit.
    - `lock-short-transactions` — el body de `check_rate_limit` no hace I/O externo, todo el lock dura microsegundos.
  - **Verificación final:** `pnpm typecheck` ✅ 0 errors · `pnpm test` para mis archivos: 30/30 ✅ (middleware + rate-limit.* x3) · `pnpm supabase test db` 99/99 ✅. Lint pre-existente roto por Node 24 + `next lint` deprecation (ver NT-29 / no relacionado a esta tarea).
- **Tareas originales fusionadas:** B13.1 (rate limiting in-DB), B13.5 (security smoke tests en CI).
