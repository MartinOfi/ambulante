# B14.1 — Crear proyecto Supabase Cloud + inyectar secrets

> **Fase:** B14 — Deploy producción
> **Goal de la fase:** Llevar el backend a Supabase Cloud, configurar secrets, pipeline de migraciones con approval, y cerrar go-live checklist.
> **Acceptance criteria de la fase:** Supabase project de prod existe con secrets inyectados; cada PR que modifica `supabase/migrations/` corre preview contra un branch; merge a main despliega via release-please; checklist de disaster recovery al día.

> **Atajos:** [INDEX](../INDEX.md) · [convenciones](../convenciones.md) · [portabilidad](../portabilidad.md) · [decisiones](../decisiones.md)

- **Estado:** ✅ done [owner: chat-2026-04-29, closed: 2026-04-29]
- **Por qué:** Prod sin proyecto no puede arrancar. Los secrets (SERVICE_ROLE_KEY, VAPID, CRON_SECRET, Google OAuth) deben estar en Vercel env vars + Supabase dashboard.
- **Entregable:** Proyecto Supabase Cloud creado, región AWS SA-East-1 (baja latencia desde Argentina). Secrets inyectados en Vercel (prod + preview) y en Supabase dashboard. Doc en `docs/runbooks/prod-setup.md`.
- **Archivos:** `docs/runbooks/prod-setup.md`.
- **Depends on:** —
- **Continues with:** —
- **Skill rules aplicables:** `conn-pooling`
- **REGISTRY:** —
- **Estimación:** M
- **Notas:** Entregable es `docs/runbooks/prod-setup.md` (206 líneas, 11 secciones: prerequisitos, inventario, crear proyecto, credenciales, Google OAuth, secrets locales, inyectar en Vercel, verificación, pitfalls, rollback, fuera-de-alcance). Cubre todas las server-only y públicas declaradas en `shared/config/env.schema.ts` excepto `EDGE_CONFIG` (documentada como opcional/inactiva). Aplica skill rule `conn-pooling`: `DATABASE_URL_POOLER` en transaction mode (6543) para serverless; `DATABASE_URL_DIRECT` (5432) sólo para CLI de migraciones. Decisiones: región `sa-east-1` (irreversible), plan Pro mínimo (`pg_cron`/`pg_net` no están en Free), previews comparten credenciales con prod hasta B14.2 (riesgo aceptado). Code review 1-pass: 0 CRITICAL, 2 HIGH (EDGE_CONFIG omitido + NT-28 duplicado en NEXT-TASK.md) + 2 MEDIUM (region slug + skill install hint), todos fixeados. Hueco detectado: `SUPABASE_WEBHOOK_SECRET` falta en `.env.example` → diferido como NT-30. Renombrado el segundo NT-28 (rutas push) a NT-31 para limpiar colisión preexistente.
