# B8.4 — Test E2E del loop completo

> **Fase:** B8 — Web Push delivery (VAPID + subscriptions + triggers)
> **Goal de la fase:** Completar el loop de notificaciones push: el browser se suscribe → guardamos la subscription → cuando cambia estado del pedido, disparamos push al subscriber correspondiente.
> **Acceptance criteria de la fase:** Tabla `push_subscriptions` con RLS correcta; endpoint de suscripción funciona end-to-end; domain event `OrderStatusChanged` dispara push; retry + cleanup de subscriptions muertas (410 Gone).

> **Atajos:** [INDEX](../INDEX.md) · [convenciones](../convenciones.md) · [portabilidad](../portabilidad.md) · [decisiones](../decisiones.md)

- **Estado:** ✅ done [owner: chat-2026-04-29, closed: 2026-04-29]
- **Por qué:** Validar que el browser realmente recibe el push no es trivial en tests unitarios. Un test E2E con Playwright + stub de service worker confirma el loop.
- **Entregable:** `e2e/push-delivery.spec.ts` que: (a) loguea un cliente; (b) suscribe al push; (c) hace submit de un pedido; (d) en otra browser context, loguea como tienda; (e) acepta el pedido; (f) verifica que el cliente recibió el push (via mock del push service captureado por Playwright).
- **Archivos:** `e2e/push-delivery.spec.ts`.
- **Depends on:** B8.3
- **Continues with:** —
- **Skill rules aplicables:** —
- **REGISTRY:** `testing.md`.
- **Estimación:** L
- **Notas:** Loop completo (subscribe → submit RECIBIDO → store accept → push capturado) implementado con infra test-only gated por `E2E_TEST_MODE === "1"`: `shared/services/push.test-capture.ts` (in-memory store + `createTestCapturePushSender`), `shared/domain/events/wiring.e2e.ts` (idempotente, registra el listener de B8.2 con sender de captura + `storeRepository` mock), `app/api/__e2e/push-capture/route.ts` (GET/POST/DELETE; 404 si flag off), `app/api/__e2e/order/route.ts` (POST crea orden directa en RECIBIDO), `instrumentation.ts` extendido para invocar el wiring en boot, `playwright.config.ts` setea el flag para todo run de Playwright. Spec `e2e/push-delivery.spec.ts` localiza la fila por `id` truncado (orderName no se renderiza en `OrderCard`). 22 unit tests nuevos en 4 archivos, todos verde. Run del spec contra dev server bloqueado por NT-30 (Node 24 + `next-config-ts/require-hook` + `server-only` en `env.runtime`) — issue pre-existente que afecta a TODAS las specs E2E, no a B8.4. Verificado: `npx tsc --noEmit` 0 errors; eslint 0 errors; vitest focalizado 66/66 (push + e2e infra). REGISTRY actualizado en `shared/REGISTRY-detail/testing.md` §16.

---
