# B11-A — Admin: dashboard KPIs + moderación (validación de tiendas + content moderation)

> **Fase:** B11 — Swap admin (features Admin consumen backend real)
> **Goal de la fase:** Reemplazar mocks del panel admin por queries reales. KPIs, validación de tiendas, moderación, audit log, user management.
> **Acceptance criteria de la fase:** Admin puede ver KPIs en vivo, aprobar/rechazar tiendas pendientes con documentos adjuntos, moderar contenido reportado, auditar pedidos históricos, gestionar usuarios.

> **Atajos:** [INDEX](../INDEX.md) · [convenciones](../convenciones.md) · [portabilidad](../portabilidad.md) · [decisiones](../decisiones.md)

- **Estado:** ✅ done [owner: chat-2026-05-05, closed: 2026-05-05]
- **Por qué:** Slice vertical "panel del admin": al loguearse ve KPIs (A3 del PRD), entra a la queue de tiendas pendientes (A1) y modera contenido reportado (A2). Las tres features comparten el shell del admin y el patrón de actions con `is_admin()` check, por lo que conviene diseñarlas con el mismo guard de auth y el mismo patrón de lista paginada.
- **Entregable:**
  1. **Dashboard KPIs:** `features/admin-kpi-dashboard/` con server-side queries agregadas (orders aceptados, tasa de aceptación, stores activos del PRD §8). Consumido por componentes con paginación / filtros básicos.
  2. **Store validation queue:** `features/admin-store-validation/` consume repository + storage (visor de docs de validación). Server Actions `approveStore`, `rejectStore(reason)` con state machine.
  3. **Content moderation:** `features/content-moderation/` consume repository de productos con flag `reported=true`. Actions `hideProduct`, `warnStore`, `suspendStore`.
- **Archivos:** `features/admin-kpi-dashboard/**`, `features/admin-store-validation/**`, `features/content-moderation/**`.
- **Depends on:** B3.1, B5.4
- **Continues with:** B11-B
- **Skill rules aplicables:** `query-composite-indexes`, `data-pagination`
- **REGISTRY:** `features.md`.
- **Estimación:** XL
- **Notas:** (se llena al cerrar)
- **Tareas originales fusionadas:** B11.1 (dashboard KPIs), B11.2 (store validation queue), B11.3 (content moderation).
